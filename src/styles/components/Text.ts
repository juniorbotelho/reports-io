import styled from "styled-components"

export const Text = styled.p`
  &.Card_Body_Text {
    font-family: sans-serif;
    font-size: 1.2rem;
    line-height: 1.618em;
    text-align: center;
    font-weight: 500;
  }
`
